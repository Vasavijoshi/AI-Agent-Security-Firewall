"""A8 — code path ignores agent/tools.py and makes a raw call via HTTP_PROXY; expect DENY,
reason=BYPASS_ATTEMPTED, logged by pep/bypass_proxy.py (M3 scope; added M2 alongside the
listener it exercises)."""

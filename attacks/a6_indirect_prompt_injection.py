"""A6 — the headline scenario: mock provider scripts the agent complying with an injected
instruction from an allowed page; enforcement contains it via taint ceiling (DB read) and
allowlist (POST to evil.com)."""
